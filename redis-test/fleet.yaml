# defaultNamespace: platform
helm:
  releaseName: claims
  repo: https://charts.bitnami.com/bitnami
  chart: redis
  version: 16.13.2
  values:
    architecture: standalone
    master:
      persistence:
        enabled: false
